<?php

beforeEach(function () {
    $this->mocked = mockFileSystems();
});

/**
 * ディレクトリコピー処理などがあるので、
 * 一旦テストを省略
 */

test('generateContent', function () {
    expect(true)->toBeTrue();
});

test('generate', function () {
    expect(true)->toBeTrue();
});
