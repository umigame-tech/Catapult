<?php

use Illuminate\Support\Facades\Route;

{% for entity in entities %}
use App\Http\Controllers\{{ entity.controllerName }};
{% endfor %}

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| Here is where you can register web routes for your application. These
| routes are loaded by the RouteServiceProvider and all of them will
| be assigned to the "web" middleware group. Make something great!
|
*/

{# for authenticated users #}
{# TODO: routeのnameにもプレフィックスを付けるようにする。各画面でroute関数を使っている箇所でも対応が必要。 #}
{% for authName, entities in authList %}
Route::prefix('{{ authName }}')->group(function () {
{% for entity in entities %}

{% for route in entity.routes %}
    {{ route|raw }}
{% endfor %}

{% endfor %}
});
{% endfor %}
{# for everyone #}
{% for entity in forEveryone %}

{% for route in entity.routes %}
{{ route|raw }}
{% endfor %}
{% endfor %}
